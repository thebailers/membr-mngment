import styled from "styled-components";

export const AuthWrapper = styled.div`
  form {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  input,
  label,
  button {
    display: flex;
    width: 400px;
  }
`;
